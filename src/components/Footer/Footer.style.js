import styled from "styled-components";

export const Footer = styled.footer`
  position: relative;
  bottom: 0;
  left: 50%;
  padding: 2rem 0;
  text-align: center;
  transform: translate(-50%, 0);
  color: white;
`;
